---
import AgencyService from '../../components/AgencyService.astro';
import {services} from '../../data/agency';
export const prerender = true;
const service = services.find(item => item.slug === 'websites')!;
---
<AgencyService service={service}/>
